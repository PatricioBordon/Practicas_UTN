#include <iostream>
///borrar carpeta bin y obj antes de entregar archivoo
using namespace std;
#include "funciones.h"
int main()
{
    int vec[10];
    for(vec=0;vec<10;i++){

    }
    cargarVector(vec,10);
    MostrarVectores(vec,10);
    return 0;
}
